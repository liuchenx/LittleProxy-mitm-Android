

name: test
on:
  workflow_dispatch

jobs:
  publish:
    runs-on: ubuntu-latest 
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: test
        run: |
          echo "start====================="
          echo "::add-mask::$GITHUB_TOKEN"



